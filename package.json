{
  "name": "maze-solver",
  "private": true,
  "version": "1.0.0",
  "description": "Solve a maze",
  "scripts": {
    "babel": "babel src --stage 1 --out-dir dist --modules system",
    "babel-watch": "babel src --stage 1 --watch --out-dir dist --modules system",
    "karma": "node node_modules/karma/bin/karma start test/karma.conf.js --single-run --no-auto-watch --reporters dots"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/joppe/maze-solver.git"
  },
  "keywords": [
    "js",
    "babeljs",
    "systemjs"
  ],
  "author": "Joppe Aarts",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.5.8",
    "babel-core": "^5.1.13"
  },
  "dependencies": {
    "babel-polyfill": "^6.5.0",
    "bower": "^1.7.9"
  }
}
